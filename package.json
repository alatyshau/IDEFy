{
  "name": "idefy-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "IDEFy — text DSL for IDEF0 modelling, VS Code extension and supporting libraries.",
  "license": "MIT",
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
