# Boundary arrows sort by group order I → C → M → O → T, then by id within group.

activity A0 "boundary sort" {
    M1 "mech"
    C1 "ctrl"
    O1 "out"
    I1 "in"
    O2 "out2"

    A1 "b" : I1, C1, M1 -> X11[O1]
}
