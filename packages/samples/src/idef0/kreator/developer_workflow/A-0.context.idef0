# Сгенерировано из Kreator.xml, экспорт Process Modeller.
# Адаптация под DSL IDEFy: контекст A-0 хранит только туннели; интерфейс
# корневого процесса перенесён в A0.work_with_kreator.idef0.

context A-0 "Контекст работы с Креатором в контексте разработчика" {
    ...A0
}
